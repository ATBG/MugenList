# MugelList Python Backend Requirements
# Install with: pip install -r requirements.txt

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0

# HTTP Client (used by metadata_service, resolver_service, anikai_resolver)
requests>=2.31.0

# HTML Parsing (used by anikai_resolver for AniKai page scraping)
beautifulsoup4>=4.12.0

# Date handling
python-dateutil>=2.8.0

# Production WSGI Servers (used for hosting on platforms like Render)
gunicorn>=21.2.0
waitress>=3.0.0
